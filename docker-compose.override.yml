services:
  api:
    ports:
      - "0.0.0.0:8000:8000"
    env_file:
      - .env.local
    extra_hosts:
      - "host.docker.internal:host-gateway"

  deriver:
    env_file:
      - .env.local
    extra_hosts:
      - "host.docker.internal:host-gateway"
